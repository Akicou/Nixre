{
  "name": "nixre",
  "private": true,
  "version": "1.0.0",
  "description": "Nixre — self-hosted Git forge, AI assistant and deploy platform. Root helper scripts only; the real packages live in backend/ and ui/.",
  "scripts": {
    "test": "npm run test:backend && npm run test:ui",
    "test:backend": "cd backend && node --test \"src/lib/*.test.js\" \"src/test/*.test.js\"",
    "test:ui": "npm --prefix ui test",
    "build:ui": "cd ui && npm run build"
  }
}
